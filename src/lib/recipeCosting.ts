// Deprecated — part of the fictional-schema recipe stack. See the comment
// at the top of src/types/recipe.ts for the full explanation. Not imported
// anywhere anymore (RecipesView.tsx / SubRecipesView.tsx compute costs
// inline against the real schema instead). Left as a stub since this
// session's shell tool couldn't delete the file outright.
export {}
