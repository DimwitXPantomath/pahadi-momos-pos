// Deprecated — built against a recipe schema that was never created in any
// Supabase migration. See the comment at the top of src/types/recipe.ts for
// the full explanation. src/pages/Index.tsx no longer imports this file —
// it renders src/components/recipes/RecipesView.tsx instead, which queries
// the real schema (001_initial_schema.sql + 011_recipe_sop_fields.sql).
// Left as a stub since this session's shell tool couldn't delete the file
// outright.
export {}
