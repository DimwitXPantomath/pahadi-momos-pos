// Deprecated: the physical/printable loyalty card was removed per product
// decision — loyalty is now QR -> online self-order (/order-online/:outletId)
// or phone number at in-store checkout, no printed card involved.
//
// This file is intentionally left as an inert stub rather than deleted:
// this session's shell tool was unavailable, so the file couldn't be removed
// from disk. It is not imported or routed anywhere (see src/App.tsx) and can
// be deleted the next time you have shell/file-manager access.
export {}
