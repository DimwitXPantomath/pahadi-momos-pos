// Deprecated — part of the fictional-schema recipe stack. See the comment
// at the top of src/types/recipe.ts. Was already unused (not imported
// anywhere in the app — IngredientsView.tsx has its own inline form and
// queries the real ingredients schema directly) before this cleanup. Left
// as a stub since this session's shell tool couldn't delete the file
// outright.
export {}
