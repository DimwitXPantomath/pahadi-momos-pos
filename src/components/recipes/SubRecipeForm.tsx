// Deprecated — part of the fictional-schema recipe stack. See the comment
// at the top of src/types/recipe.ts. Not imported anywhere anymore
// (SubRecipesPage.tsx, its only caller, has also been deprecated —
// src/pages/Index.tsx now renders src/components/subrecipes/SubRecipesView.tsx
// instead). Left as a stub since this session's shell tool couldn't delete
// the file outright.
export {}
